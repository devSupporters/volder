export const inputValidator = (
  input: any,
  key:string,
  validatorOption: object,
  validInput:boolean,
  errors: object
) => {
    // the operations 
    // 1 => the type is true
    // 2 => the min length true in (number | string)
    // 3 => the max length true in (number | string)
};
