export const setupDefaultConfig = (optionConfigs, type) => {
    if(optionConfigs.hasOwnProperty('default')) {
        // for every sinel type validate it 
        // throw an error if it wrong
    }
}